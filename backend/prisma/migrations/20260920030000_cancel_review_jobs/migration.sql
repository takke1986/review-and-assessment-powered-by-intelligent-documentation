-- 審査を途中で止められるようにする。
-- 文書を間違えて開始しても、これまでは終わるまで待って費用も払うしか
-- なかった。
--
-- 実行の識別子を控えておき、走っている最中でも止められるようにする。
-- まだ待ち行列にいる場合は識別子が無いので、状態を見て始めない。
ALTER TABLE `review_jobs` ADD COLUMN `execution_arn` VARCHAR(255) NULL;
