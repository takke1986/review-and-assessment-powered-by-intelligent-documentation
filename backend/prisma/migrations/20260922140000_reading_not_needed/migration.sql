-- 先読みが要らなかった書類も記録する
--
-- 小さい書類は先読みを通らない（審査のときに元のファイルを直接渡せるため）。
-- これまでは記録そのものが作られなかったので、
--
--   先読みが不要だった（正常）
--   先読みは走ったが記録に失敗した（異常）
--
-- が区別できなかった。どちらも「行が無い」に見える。not_needed として
-- 残せるようにする。置き場所は無いので s3_key は任意にする。
ALTER TABLE `review_document_digests` MODIFY `s3_key` VARCHAR(1024) NULL;
